MODEL_NAME=gpt-4o-mini

# Server Configuration
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# CORS (comma-separated list)
ALLOWED_ORIGINS=http://localhost:8081,exp://localhost:8081,http://localhost:19006

# Logging
LOG_LEVEL=INFO

# Security
# IMPORTANT: Generate a secure SECRET_KEY for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
# This key is used for JWT token signing - keep it secret!
SECRET_KEY=d9F2oSC-uEphXSwo4wqvalRmztQh8p0B1bHl9WHGTsk

# Rate Limiting
RATE_LIMIT_ENABLED=true